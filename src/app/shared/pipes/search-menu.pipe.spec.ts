import { SearchMenuPipe } from './search-menu.pipe';

describe('SearchMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
