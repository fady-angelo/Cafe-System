import { SearchMeneByCatPipe } from './search-mene-by-cat.pipe';

describe('SearchMeneByCatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMeneByCatPipe();
    expect(pipe).toBeTruthy();
  });
});
